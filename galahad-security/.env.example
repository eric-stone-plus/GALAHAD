# Copy to .env and fill only if enabling the Alpaca PAPER venue path
# (default: offline paper book, no keys needed). NEVER commit .env.

# Alpaca PAPER-trading keys (paper account on the Alpaca dashboard) — read ONLY
# by the venue engine (--engine alpaca_paper / scripts/run_venue.py).
# PAPER ONLY: this package has no live-money path; never put live-trading
# credentials in these variables.
# ALPACA_PAPER_API_KEY=
# ALPACA_PAPER_API_SECRET=
